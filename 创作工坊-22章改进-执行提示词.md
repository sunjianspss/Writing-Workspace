# 创作工坊 · 第 22 章改进执行提示词

日期：2026-07-04
用途：把 `创作工坊-PRD.md` 第 22 章的改进建议，拆成可以直接粘贴给 Claude Code（或其他编码代理）的任务提示词。按顺序执行，一个会话跑一个任务，任务之间由你审查 diff 后再继续。

---

## A. 先花三分钟理解：这些提示词为什么长这样

你说对"agentic harness 约束架构"不了解——其实核心只有四条，本文档每个任务提示词都是按这四条写的，你以后自己写任务提示词也照这个骨架即可：

1. **说清"完成"长什么样（验收标准）**：不写"优化一下 X"，写"改完之后，用命令 Y 检查，应该看到 Z"。代理有了可检查的终点，就不会自由发挥。
2. **给证据锚点（文件与符号名）**：直接指出问题在哪个文件的哪个类型/函数里。代理不用猜，也不会改错地方。
3. **划边界（明确不做什么）**：约束比要求更重要。"不要改 prompt 文案""不要动待复核状态机"这类禁令，防止代理顺手重构、越改越多。
4. **要求验证，而不是相信声明**：每个任务结尾都要求跑测试和护栏脚本，并要求代理如实报告失败。没跑过验证的"已完成"不算数。

---

## B. 通用约束块（每个任务开始前，先把这一块粘贴给代理）

```text
【项目通用约束——先读完再动手】

项目：/Users/sun/Documents/Writing-Workspace（macOS 原生写作应用，Swift 代码在 macos/CreativeWorkshopMac）。
架构与产品背景见 ARCHITECTURE.md 和 创作工坊-PRD.md 第 22 章（本任务出自该章的审查结论）。

硬性边界（违反任何一条即任务失败）：
1. 所有 AI 工作流必须走 WorkflowDescriptor / WorkflowEngine / NativeWorkflowCatalog，不得绕过；
   视图层不得直接引用 NativeAIClient、ModelGateway、AIWorkflowRunner。
2. 不改动任何 prompt 文案，除非本任务明确要求；改动时逐字按任务给出的文案执行。
3. 不破坏待复核状态机：pendingDraftReview 未确认时阻塞保存的语义不得改变。
4. 不引入第三方依赖，不改数据库既有表结构（新增表/字段需走 PRAGMA user_version 迁移，且只在任务明确要求时做）。
5. 保留"无 API Key 演示模式"：完全没有配置 API Key 时，现有的本地 fallback 流程必须照常可用。
6. 最小改动：与任务无关的代码、注释、格式一律不动；发现无关问题只在报告里提一句，不顺手修。

完成定义（每个任务通用）：
a. 编译与全部测试通过：
   env SWIFTPM_CONFIG_PATH="$PWD/.swiftpm-state/config" \
     SWIFTPM_SECURITY_PATH="$PWD/.swiftpm-state/security" \
     SWIFTPM_CACHE_PATH="$PWD/.swiftpm-state/cache" \
     CLANG_MODULE_CACHE_PATH="$PWD/.swiftpm-state/module-cache" \
     swift test --disable-sandbox --package-path macos/CreativeWorkshopMac
b. 护栏脚本通过：./script/architecture_guard.sh
c. 本任务的每条验收标准都有对应的自动化测试或可复述的验证步骤。
d. 最后如实报告：改了哪些文件、验证结果原文、哪些验收项未覆盖及原因。测试失败时报告失败，不得为通过而放宽断言或跳过测试。
```

---

## C. 任务提示词（按此顺序执行）

### 任务 1：修复"全量语料库泄漏进 prompt"（PRD 22.3.1，P0，最先做）

```text
任务：修复 ContextPackage 把整库素材片段序列化进 AI prompt 的缺陷。

问题证据（先逐一核实再动手）：
- WorkshopStore.quickDraft 把 rebuildFragments() 返回的全部片段赋给 context.fragment_corpus。
- ContextPackage（WorkshopModels.swift）是自动合成的 Codable，没有排除字段，
  所以 fragment_corpus、section_fragment_contexts、retrieved_fragments 全部会被编码。
- NativePrompts 里 writingBrief、argumentCheck、draftCritique 三个 prompt 都内嵌完整的
  contextJSON(context)。结果是：每次"代理式初稿"，整个素材库被塞进 3 步调用的 prompt，
  库越大 token 越爆炸，最终超上下文窗口导致整条管线失败。

修复要求：
1. 让 fragment_corpus、section_fragment_contexts、retrieved_fragments 三个字段不参与
   ContextPackage 的 Codable 编码（自定义 CodingKeys 排除；注意被排除的存储属性需要显式
   默认值，如 `var fragment_corpus: [Fragment]? = nil`）。
2. 动手前先全库检索这三个字段和 ContextPackage 编解码的所有使用点（包括数据库是否有
   持久化过 ContextPackage JSON 的地方），确认排除编码不会破坏任何读取方；有疑点在报告中列出。
3. fragment_corpus 供 AgentDraftCoordinator 做按段二次检索、section_fragment_contexts 供
   sectionFragmentsBlock 显式注入——这两条内存中的使用路径必须保持原样，不受影响。
4. 不改任何 prompt 文案。修复后 prompt 里的【当前写作上下文 JSON】自然不再含整库内容即可。

验收标准（每条都要有测试）：
1. 新增单元测试：构造含 50 个 Fragment 的 ContextPackage，编码为 JSON 后断言不包含任何
   片段正文内容，且 JSON 长度与片段数量无关。
2. 新增或扩展测试：断言 NativePrompts.writingBrief / argumentCheck / draftCritique 生成的
   消息文本不包含语料库片段内容（用带特殊标记串的假片段验证）。
3. 现有全部测试通过，architecture_guard.sh 通过。
```

### 任务 2：按工作流的默认采样参数与输出保障（PRD 22.3.4 + 22.3.2，P0）

```text
任务：为不同类型的 AI 工作流设置代码级默认参数（温度 / 超时 / max_tokens），
解决"判别任务用 0.75 高温采样"和"长输出无 max_tokens、90 秒即超时"两个缺陷。

背景证据（先核实）：
- ModelGatewayRequest 默认 temperature 0.75、timeoutSeconds 90（ModelGateway.swift）。
- ChatCompletionRequest 没有 max_tokens 字段，ModelRouteConfig（WorkshopModels.swift）也没有
  maxTokens，即目前连配置通道都不存在。
- 后果：写作诊断/候选评委/终审等判别任务被高温噪声干扰（DeepDraft 的"±3 分收敛判据"
  小于采样波动）；长文生成被服务商默认输出上限截断导致 JSON 解析失败，或 90 秒超时。

实现要求：
1. ModelRouteConfig 增加 `maxTokens: Int?`；ChatCompletionRequest 增加 max_tokens
   （编码键名 "max_tokens"，nil 时不编码该键）。
2. 在 AIWorkflowKind 上增加代码级默认值（computed property，不写入数据库、不动 settings）：
   - 判别类（writingReview、candidateJudge、prePublishAudit、draftSelfCheck、pitfallSummary、
     editPreferenceSummary、writingAdvisor、readerPerspective）：temperature 0.2，
     maxTokens 4096，timeout 120 秒。
   - 生成类（agentDraft、draft、polishDraft、improveFromReview、deepDraft、rewriteSelection）：
     temperature 0.75，maxTokens 8192，timeout 240 秒。
   - 其余（topics、outline、publishAssets）：temperature 0.75，maxTokens 4096，timeout 120 秒。
3. 参数解析优先级必须是：用户 workflowOverrides（settings 里的 JSON）> 工作流代码级默认 >
   现有全局默认。用户已配置的 overrides 行为一丝不变。
4. 改动集中在 ModelGateway / ModelConfig 解析路径，不改任何 prompt，不改 UI。

验收标准：
1. 单元测试：无 override 时，writingReview 请求的 temperature 为 0.2、draft 类请求的
   timeout 为 240、max_tokens 正确编码进请求体 JSON；有 override 时 override 胜出；
   maxTokens 为 nil 时请求体不含 "max_tokens" 键。
2. 现有全部测试通过，architecture_guard.sh 通过。
```

### 任务 3：解析失败重试 + 代理管线熔断（PRD 22.3.3，P0）

```text
任务：两个独立但相关的可靠性修复——
(A) 模型输出解析失败时重试一次；(B) 多步管线中间步失败时熔断，禁止 fallback 模板产物
流入下游模型步骤。

背景证据（先核实）：
- AIWorkflowRunner.run 任何错误一次即降级为本地 fallback，无重试。
- AgentDraftCoordinator（brief → 论点检查 → 分段成稿 → 自我批评）对中间步失败不熔断：
  分段成稿失败时，"自我批评与整合"会把 NativeFallbacks 的模板稿当真稿整合，产出一篇
  模板腔"初稿"进入待复核。
- DeepDraftCoordinator 在诊断调用失败时不中断，会用 NativeFallbacks.writingReview 的
  启发式结果继续驱动修订，甚至把 fallback 改稿（正文里附着"本地兜底"说明段）写回
  currentContent。PRD 20.2.5 要求的语义是"任何一环失败，直接以当前最好的版本进入待复核"。

实现要求：
(A) 解析重试（AIWorkflowRunner）：
1. 仅当错误属于输出解析类（NativeAIError.invalidJSON、NativeAIError.emptyContent、
   Swift DecodingError）时重试一次：在原 messages 后追加两条消息——
   assistant 角色带上一次原始输出（超过 2000 字符则截断），user 角色为：
   "你上一次的输出无法解析为要求的 JSON（错误：{解析错误摘要}）。请重新输出，
   只输出符合要求的合法 JSON，不要包含 Markdown 代码块、解释或任何其他内容。"
2. 网络错误、HTTP 错误、取消不重试，行为保持现状。重试仍失败才走 fallback。
   重试发生与否记入 AIRun 的 error/inputSummary 摘要，便于在 ai_calls 里排查。

(B) 管线熔断（AgentDraftCoordinator + DeepDraftCoordinator）：
3. 必须区分两种失败：错误为 NativeAIError.missingAPIKey（未配置 Key 的演示模式）时，
   保持现有"全程 fallback 演示"行为完全不变；其余失败（网络/HTTP/解析）才熔断。
4. AgentDraftCoordinator：某一步失败即停止管线，不再调用后续步骤。返回值携带已完成
   步骤的 trace 和失败信息；WorkshopStore.quickDraft 对熔断结果不调用
   finalizeGeneratedDraft（即不产生待复核版本、不改动当前正文），改为在 statusText 提示
   "代理式初稿在第 N 步（步骤名）失败：{原因}"，并照常 recordAgentRun 留痕。
5. DeepDraftCoordinator：诊断步失败 → 立即 break，以当前最好版本收尾；修订步失败 →
   不把 fallback 稿写回 currentContent / bestDraft，立即 break。两种情况 stoppedReason
   都写明"第 N 轮{诊断/修订}调用失败，提前结束"，不得复用"无高/中严重度问题"等
   正常退出文案。
6. 现有 DeepDraft 相关测试（如 testDeepDraftReturnsCurrentDraftWhenReviewFails）按新语义
   审查更新：断言的目标行为以 PRD 20.2.5 为准。

验收标准：
1. 单元测试（用假 gateway/假 executor）：首次输出为非法 JSON、重试输出合法 → 结果成功
   且只多一次调用；两次都非法 → 走 fallback；HTTP 错误 → 不重试。
2. 单元测试：分段成稿步失败（非 missingAPIKey）时，critique 步骤不被调用，
   quickDraft 不产生 pendingDraftReview，正文未被改动。
3. 单元测试：missingAPIKey 时四步全部 fallback、照常产出演示稿进入待复核（现状保持）。
4. 单元测试：DeepDraft 诊断步失败即停轮，返回的 draft 与进入时一致，stoppedReason 为失败文案。
5. 现有全部测试通过，architecture_guard.sh 通过。
```

### 任务 4：候选评委顺序随机化（PRD 22.3.4 后半，P0 收尾，小任务）

```text
任务：消除候选评委的位置偏置。

背景：WorkshopStore.generateDraftAlternatives 按固定顺序（natural、tighten、deepen）生成
3 个候选并按同样顺序交给 candidateJudge 评委 prompt，LLM 评委存在位置偏置（偏向首位）。

要求：
1. 送入 NativePrompts.judgeDraftCandidates 前打乱候选顺序；candidate_index 保持稳定标识
   （打乱的是呈现顺序，不是编号），评委返回的 best_candidate_index 仍按编号解析。
2. 呈现顺序记入该步 AgentStepPayload 的 input_summary（如"呈现顺序：2,3,1"），便于复盘。
3. 随机源可注入（init 参数默认 SystemRandomNumberGenerator），保证测试可用固定种子复现。
4. 不改评委 prompt 文案，不改 UI。

验收：单元测试验证打乱后编号与内容对应关系不变、可注入固定种子复现顺序；全部测试与护栏通过。
```

### 任务 5：让论点检查的裁决真正生效（PRD 22.2.1，P1，含新 prompt）

```text
任务：AgentDraftCoordinator 目前拿到论点检查结果后无条件进入分段成稿——
ArgumentCheckResult.ready_to_draft 与 missing_evidence 在全库没有任何控制流消费
（只有 WorkshopModels.swift 里的字段定义和 prompt 构造）。把它变成真正的门。

要求：
1. 在 NativeWorkflowCatalog / NativePrompts 新增 briefRevision 工作流（kind 复用 .agentDraft，
   endpoint 为 "agent-draft-brief-revision-native"），prompt 文案逐字如下（system 与既有
   writingBrief 一致："你是严谨的中文写作代理，只输出用户要求的 JSON。"）：

   你是我的中文长文写作代理。论点编辑判断当前 writing brief 还不足以支撑成稿。
   请只修订 brief，不要写正文。

   【原 writing brief JSON】
   {原 brief JSON}

   【论点检查 JSON】
   {论点检查 JSON}

   【可用素材】
   {素材内容}

   修订要求：
   1. 逐条回应论点检查中的 revision_directives，能用现有素材补上的缺口，直接写进
      material_strategy 和 structure_plan
   2. missing_evidence 中现有素材无法覆盖的项，原样放进 unresolved_gaps，
      不要编造经历或案例去填补
   3. 不要扩大文章野心，宁可收窄核心问题，也不要留下撑不起来的主张

   请只输出 JSON，不要输出 Markdown 代码块：结构与原 brief 完全相同，
   另加一个字段 "unresolved_gaps": ["现有素材仍无法覆盖的证据/场景缺口"]

   （fallback：返回原 brief 原样 + unresolved_gaps = missing_evidence，在 NativeFallbacks 新增对应函数。）

2. AgentDraftCoordinator 逻辑：论点检查后，若 ready_to_draft == false 或 missing_evidence
   非空，则执行一轮（最多一轮，禁止循环）briefRevision，用修订后的 brief 继续走分段成稿；
   该步作为独立 AgentStepPayload 记入 steps（名称"Brief 修订"）。
3. WritingBriefResult 增加可选字段 unresolved_gaps；AgentDraftTrace 透传；待复核卡片
   （InspectorView 的 agentTrace 区域）在 unresolved_gaps 非空时列出，标题
   "素材缺口（建议补素材后重新生成）"。
4. AgentDraftQualityGateEvaluator 增加第六项：unresolved_gaps 非空 → review 状态，
   detail 列出缺口条数。

验收标准：
1. 单元测试（假 executor）：ready_to_draft=false 时 briefRevision 被调用且只调用一次，
   steps 里多出"Brief 修订"；ready_to_draft=true 且 missing_evidence 为空时不调用。
2. 解码测试：unresolved_gaps 缺失时兼容旧 JSON（可选字段）。
3. 质量门测试：unresolved_gaps 非空时第六项为 review。
4. 全部测试与护栏通过。
```

### 任务 6：质量门语义化（PRD 22.2.2，P1，零模型成本）

```text
任务：AgentDraftQualityGateEvaluator 现有五项检查全部是"字段非空"判断，
劣质产出可全绿通过。增加本地可计算的语义信号，不新增任何模型调用。

要求（在 AgentDraftQualityGateEvaluator 增加三项本地检查，输入沿用现有 trace/selfCheck，
必要时给 evaluate 增加参数传入最终正文与 brief 的 structure_plan）：
1. 结构偏差：最终正文的实际小节数（按 Markdown 二级标题或空行段落估算，与现有
   sectionSummaries 数量交叉）与 brief.structure_plan 计划段数偏差 ≥2 → review。
2. 长度完整性：正文字符数低于计划段数 × 300 的下限，或正文结尾不是句末标点
   （。！？…"」』等，疑似截断）→ review。
3. 雷区特征扫描：style.known_pitfalls 逐条与正文做包含匹配之外，另内置一组结尾套话
   模式（正则，如「愿我们都…」「与君共勉」「你学会了吗」「点个赞」「关注我」等
   常见公众号套话收尾），命中 → review 并给出命中片段。
4. 每项 detail 必须写清具体数字或命中片段，让作者不用点开正文就知道该看哪里。
5. 纯函数实现，全部可单元测试；不改 prompt，不改协调器流程。

验收标准：
1. 每项新检查至少两条测试（触发 review / 通过 passed 各一）。
2. 构造一份"字段全填但正文只有两句话"的劣质 trace，质量门必须出现 review 项
   （这是对 22.2.2 缺陷的直接回归测试）。
3. 全部测试与护栏通过。
```

### 任务 7：雷区命中改为模型显式输出 + 深度循环换主判据（PRD 22.4.3，P1，含 prompt 修改）

```text
任务：两个配套修改——
(A) DeepDraftCoordinator.pitfallHits 里 content.localizedStandardContains(description) 的
正文直配检查近乎无效（雷区描述是建议句式，如"少用金句收束"，不会字面出现在正文里），
改为让写作诊断 prompt 显式输出命中结果；
(B) 深度成稿循环的主判据从"同模型自评分"改为"上一轮问题核销率"。

要求：
1. 修改 NativePrompts.writingReview 的默认 prompt（以及 defaultPromptTemplates 里
   writingReview 模板种子）：
   a. 诊断要求追加一条（放在现有第 8 条之后）：
      "9. 如果作者风格说明列出了"作者常见雷区"，逐条判断本文是否再次命中：
       命中的雷区写入 pitfall_hits（原样抄写雷区描述），未命中不要写。"
   b. 输出 JSON 结构增加字段："pitfall_hits": ["命中的雷区原文描述，未命中留空数组"]。
2. WritingReviewResult / WritingReview 增加可选字段 pitfall_hits（解码兼容旧数据，
   数据库 writing_reviews 若按 JSON 存储 raw 结果则无需迁移；如需列级存储，走
   user_version 迁移）。
3. DeepDraftCoordinator.pitfallHits：改为优先使用 review.pitfall_hits；保留现有
   issue 文本匹配作为兜底；删除对正文的 localizedStandardContains 直配分支。
4. 换主判据：循环继续/停止的首要条件改为——
   上一轮高/中严重度 issues 的核销率（本轮 resolved_from_last 覆盖的条数 ÷ 上一轮
   高+中 issue 总数）：核销率 ≥ 0.8 且无新增高严重度 issue 且无 pitfall_hits → 停止；
   连续两轮核销率 < 0.3 → 判定空转，停止并写明原因。
   overall_score 降级为迭代摘要里的展示信息，不再参与停止判定（删除"达到目标分"
   与"±3 分收敛"两个分支，DeepDraftInput.targetScore 保留字段但标注废弃或移除，
   以最小改动为准）。
5. DeepDraftIteration 的 stoppedReason 文案同步更新，待复核卡片展示不变形。
6. 现有 DeepDraft 三条测试按新判据更新断言；新增核销率停止/空转停止各一条测试。

验收标准：
1. 解码测试：含/不含 pitfall_hits 的诊断 JSON 均可解码。
2. 循环测试：构造假评审序列验证"核销率 ≥0.8 停止""连续低核销率空转停止"
  "pitfall_hits 非空不得停止"三种路径。
3. 全部测试与护栏通过。
```

### 任务 8：最小写作评测集（PRD 22.4.2，P1，本轮最高杠杆）

```text
任务：建立最小可用的写作质量评测（eval harness），让"改造是否提高了写作质量"
第一次变得可测量。只做 CLI + 本地存储 + Markdown 报告，不做 UI。

设计约束：
1. 评测用例：新建 evals/cases/ 目录，每个用例一个 JSON 文件，结构：
   { "id": "case-01", "kind": "idea|draft", "idea": "...", "direction": "...",
     "materials": "...", "content": "（kind=draft 时的既有初稿）" }
   先放 3 个占位示例用例（内容用通用示例，作者之后替换为真实历史输入）。
2. 评测 runner：SwiftPM 新增 executable target（如 CreativeWorkshopEval），复用主 target
   的 NativeWorkflowCatalog / 协调器 / NativeDatabase，绝不复制粘贴管线代码。
   每个用例按配置跑指定管线：direct（draft 工作流一步成稿）、agent（AgentDraftCoordinator）、
   deep（agent 后接 DeepDraftCoordinator）。
3. 评分：复用 writingReview 工作流对每份产出打分（走任务 2 之后的判别类低温默认），
   记录 overall_score、高/中/低 issue 计数、字数、调用次数、总耗时。
4. 存储：结果写入独立的 eval SQLite（evals/eval_results.sqlite3，独立于主库，
   不动主库 schema），表含 run_id、时间戳、git 描述、case_id、pipeline、各项指标、原始 JSON。
5. 报告：跑完输出 evals/reports/{时间戳}.md，一张对照表（行=用例，列=管线×指标），
   末尾附与上一次 run 的差值。
6. API Key 从现有 KeychainCredentialStore 读取；无 Key 时明确报错退出，不静默跑 fallback
   （评 fallback 没有意义）。
7. 运行方式写进 README 的验证一节：
   swift run --package-path macos/CreativeWorkshopMac CreativeWorkshopEval --pipelines direct,agent

验收标准：
1. swift build 通过，eval target 不被主 App 依赖（App 体积与行为零变化）。
2. 用假 executor 的单元测试覆盖：用例加载、结果写库、报告生成（不真实调用模型）。
3. 报告文件可读：一眼能对比同一用例三条管线的分数与成本。
4. 全部测试与护栏通过。
```

### 任务 9（后置，与 19.3.5 合并排期）：流式输出

```text
任务：实现 ModelGateway.stream（目前是抛错 stub，全库无调用方），让生成类工作流走流式。
建议单独排期，改动面涉及 runner、engine、Store 的增量 UI 更新与取消语义，
先完成任务 1–8 并稳定后再做。做之前把本任务连同通用约束块一起交给代理，
并要求它先给出实施方案再动代码（方案里必须说明：增量文本如何进入 JSON 解析、
取消如何传播、待复核语义如何保持）。
```

### 任务 10（文档，随时可做）：PRD 状态回填

```text
任务：只改文档，不改代码。按 创作工坊-PRD.md 第 22.6 节的漂移清单回填状态：
1. 第 20 章各节（20.2/20.3/20.4/20.5/20.6）按第 18 章的格式补"实施记录"，
   写明落地文件（DeepDraftCoordinator.swift、FragmentRetriever.swift、edit_records、
   PrePublishAuditCoordinator.swift、workflowOverrides 等），状态行从"提案，尚未实施"
   改为"已实施，含后续修订"。
2. 第 21 章标注 R1–R4 已完成、R5 进行中（WorkshopStore 当前行数与护栏线）。
3. PRD_完成度审计.md 的"后续不在本轮范围"更新：模型路由已实现，流式仍未实现。
4. 第 22 章各任务完成后，在对应小节追加一行"实施记录：{日期}，见任务 N"。
5. 不改动任何未提及章节的文字。
```

---

## D. 使用建议

1. **顺序执行，别并行**：任务 1–3 是 P0，彼此有依赖（任务 2 的低温默认是任务 7 评分可信的前提；任务 3 的熔断语义影响任务 8 评测的解读）。
2. **每个任务一个新会话**：粘贴 B 节通用约束块 + 对应任务提示词即可，提示词是自包含的。
3. **你只做两件事**：任务开始前确认提示词里的"要求"是你想要的；任务结束后看代理的报告和 diff——重点看它是否如实报告了测试结果、有没有动边界外的东西。
4. **任务 8 跑通后**，先用占位用例把三条管线各跑一轮留底，再换成你的真实历史输入——之后每完成一个任务重跑一次，第 22 章"质量有没有提高"的问题从此有数据回答。
