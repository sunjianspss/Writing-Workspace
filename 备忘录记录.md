 现在系统已经完成了，还有什么备忘录需要让我留意的吗？

---

好问题——收官时刻正适合立备忘。我先把关键事项写进我的持久记忆（以后新会话自动带上，不用你重新解释项目），然后给你一份人读的留意清单：

  Wrote 4 memories

记忆已存（以后任何新会话我都会自动带着项目状态、评测惯例和你的偏好，不用重新解释）。下面是给你本人的留意清单，按性质分四类：

一、日常使用（现在就要记住的）

1. 跑评测永远在自己的终端跑，别在 Claude Code 会话里跑——评测烧的是 DeepSeek（便宜），但挂在会话下会因 Claude 额度耗尽被杀（已发生两次）。命令在 README 验证一节，前面加 caffeinate -i。
2. 跑评测时别关终端窗口；合盖没关系（韧性已内建），尽量插电。
3. 加评测用例的姿势：长正文用 jq -Rs '.' 文章.md 转义，JSON 没有三引号；新题材按"一鱼两吃"建一对；编号先 ls 避免撞号。

二、悬置事项（条件满足时触发）

┌─────────────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────┐
│          事项           │                                              触发条件                                               │
├─────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 任务 18（调度偏好记忆） │ 实验室开代理会话，真实使用攒够 5 条会话样本                                                         │
├─────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ L2 转默认重议           │ 管线/prompt 实质改动、或新用例批量入库后，重跑评测看放行门                                          │
├─────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 北极星指标出数          │ 用 App 完成"确认稿→发布"闭环，edit_records 才有数据——这是整个系统的终极考题，只有你的真实写作能回答 │
└─────────────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────┘

三、维护债（不急，但别忘）

1. WorkshopStore 已 2686 行，超 2600 警戒线（护栏每次构建都会警告）——下次加功能前先还一点 R5 瘦身债，别等它滚到 3000。
2. 文档惯例已立：任何新改动都要在 PRD 对应章节回填"实施记录"——这个项目的 PRD 之所以可信，靠的就是这条纪律。

四、数据与安全

1. 仓库必须保持 private——评测用例里是你的完整文章。
2. evals/eval_results.sqlite3 被 gitignore，是本机数据，换电脑会丢；报告（evals/reports/）已入库无虞。在意历史结果的话定期备份那个文件。
3. API Key 只在 Keychain；.env、data/、exports/ 永不入库（已配置，别手动 force add）。

最后一句真心话：这套系统从审查到收官，所有该自动的都自动了、该留档的都留档了——它现在唯一的短板是"没被真正用来写作过几篇文章"。第二类清单里的三件事，本质上是同一件事：打开 App，写下一篇。

---

本地测评命令：

```bash
cd /Users/sun/Documents/Writing-Workspace
caffeinate -i env SWIFTPM_CONFIG_PATH="$PWD/.swiftpm-state/config" \
  SWIFTPM_SECURITY_PATH="$PWD/.swiftpm-state/security" \
  SWIFTPM_CACHE_PATH="$PWD/.swiftpm-state/cache" \
  CLANG_MODULE_CACHE_PATH="$PWD/.swiftpm-state/module-cache" \
  swift run --package-path macos/CreativeWorkshopMac CreativeWorkshopEval
```
想跑子集就加 --cases case-16,case-17、只跑关键管线加 --pipelines deep,agentic。

例如：

```bash
cd /Users/sun/Documents/Writing-Workspace
caffeinate -i env SWIFTPM_CONFIG_PATH="$PWD/.swiftpm-state/config" \
  SWIFTPM_SECURITY_PATH="$PWD/.swiftpm-state/security" \
  SWIFTPM_CACHE_PATH="$PWD/.swiftpm-state/cache" \
  CLANG_MODULE_CACHE_PATH="$PWD/.swiftpm-state/module-cache" \
  swift run --package-path macos/CreativeWorkshopMac CreativeWorkshopEval \
  --cases case-18,case-19,case-20,case-21,case-22,case-23,case-24
```



