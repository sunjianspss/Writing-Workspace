# 创作工坊 · 第 23 章改进执行提示词（任务 11–19）

日期：2026-07-05
用途：把 `创作工坊-PRD.md` 第 23 章（Agentic Harness 演进提案）拆成可直接粘贴给 Claude Code（或其他编码代理）的任务提示词。编号承接第 22 章的任务 1–10。按顺序执行，一个会话跑一个任务，任务之间由你审查 diff 后再继续。

---

## A. 先读这一段：这次你是在"用一个 harness 造另一个 harness"

约束式提示词的四条骨架和上次一样（详见 `创作工坊-22章改进-执行提示词.md` 的 A 节）：**说清"完成"长什么样、给证据锚点、划边界、要求验证**。

这次多一层有意思的对应关系，理解它你就理解了 agentic harness：本文档的任务提示词约束的是"编码代理"，而任务要构建的系统约束的是"写作代理"——两者是同一套思想的两次应用：

| 本文档对编码代理的约束 | 第 23 章对写作代理的约束 |
| --- | --- |
| 硬性边界（不许动什么） | 封闭动作空间（只能选枚举内的动作） |
| 验收标准（完成的定义） | 验证器信号（质量门/自检/诊断） |
| 要求跑测试、如实报告 | 每步产物入轨迹、决策 JSON 可审计 |
| 你审查 diff 后才继续下一任务 | 一切产物过待复核，作者定稿 |
| 一个任务一个会话（预算隔离） | 每会话调用预算，耗尽必交付 |

所以执行这批任务时你自己就在体验 harness 的工作方式：**自治发生在检查点之间，检查点永远归人。**

---

## B. 通用约束块（每个任务开始前，先把这一块粘贴给代理）

```text
【项目通用约束——先读完再动手】

项目：/Users/sun/Documents/Writing-Workspace（macOS 原生写作应用）。
模块布局（第 22 章任务后已拆分）：
- macos/CreativeWorkshopMac/Sources/CreativeWorkshopCore：模型、服务、协调器、prompt、数据库（业务核心，无 UI）
- macos/CreativeWorkshopMac/Sources/CreativeWorkshopMac：App、视图、WorkshopStore
- macos/CreativeWorkshopMac/Sources/CreativeWorkshopEval：评测可执行目标（依赖 Core，不被主 App 依赖）
- 测试：Tests/CreativeWorkshopMacTests 与 Tests/CreativeWorkshopEvalTests
产品背景见 创作工坊-PRD.md 第 23 章（本任务出自该章）与第 22 章（已实施的前置改造）。

硬性边界（违反任何一条即任务失败）：
1. 所有 AI 工作流必须走 WorkflowDescriptor / WorkflowEngine / NativeWorkflowCatalog；
   视图层不得直接引用 NativeAIClient、ModelGateway、AIWorkflowRunner；
   协调器内不得手写 prompt（prompt 只能在 NativePrompts 与 prompt_templates）。
2. 不改动任何既有 prompt 文案，除非本任务明确要求；新增 prompt 逐字按任务给出的文案执行。
3. 不破坏待复核状态机：pendingDraftReview 未确认时阻塞保存/生成的语义不得改变。
4. 第 23 章五条设计律（PRD 23.3）是每个任务的默认约束：
   a. 语料不进决策上下文，只进检索结果（观察必须压缩）；
   b. fallback 产物不进下游模型步骤；
   c. 判别低温、生成高温（沿用 AIWorkflowKind.defaultParameters 机制）；
   d. 每步可验证、可追溯（决策与产物入 agent_runs / agent_steps）；
   e. 一切自治在待复核之内（finish 永远合法，预算耗尽必交付，不空手）。
5. 遵守 21.5 护栏：新功能一律不得在 WorkshopStore 里手写编排；编排放独立协调器（Core），
   Store 只加状态绑定与转发。WorkshopStore.swift 当前 2635 行已超警戒线，本轮任务净增不得超过 +80 行。
6. 数据库变更必须走 PRAGMA user_version 迁移，历史数据可无损升级，并在 NativeDatabaseTests 补迁移测试。
7. 不引入第三方依赖。最小改动：与任务无关的代码、注释、格式一律不动；发现无关问题只在报告里提一句。
8. 不要编造作者的个人数据（评测用例、素材、历史文章）；需要真实数据的地方留占位并写清楚作者该如何补。

完成定义（每个任务通用）：
a. 编译与全部测试通过：
   env SWIFTPM_CONFIG_PATH="$PWD/.swiftpm-state/config" \
     SWIFTPM_SECURITY_PATH="$PWD/.swiftpm-state/security" \
     SWIFTPM_CACHE_PATH="$PWD/.swiftpm-state/cache" \
     CLANG_MODULE_CACHE_PATH="$PWD/.swiftpm-state/module-cache" \
     swift test --disable-sandbox --package-path macos/CreativeWorkshopMac
b. 护栏脚本通过：./script/architecture_guard.sh
c. 本任务的每条验收标准都有对应的自动化测试或可复述的验证步骤。
d. 最后如实报告：改了哪些文件、验证结果原文、哪些验收项未覆盖及原因。
   测试失败时报告失败，不得为通过而放宽断言或跳过测试。
```

---

## C. 任务提示词（按 PRD 23.12 顺序执行）

### 任务 11：真实评测用例支撑与放行门（PRD 23.4，P0，先立闸门）

```text
任务：为评测系统补上"放行门"判定与真实用例的接入支撑。这是第 23 章其余任务的闸门：
L2 代理循环是否值得设为默认，由本任务建立的判定说了算。

背景（先核实）：
- 评测目标已存在：Sources/CreativeWorkshopEval（main.swift / PipelineRunner.swift /
  ReportGenerator.swift / EvalResultsStore.swift / EvalCase.swift），支持 --pipelines 参数，
  报告落 evals/reports/。
- evals/cases/ 目前是 3 个占位用例。真实用例必须由作者本人提供（历史想法/初稿），
  你不得编造。

实现要求：
1. 用例校验：加载用例时做结构校验（必填字段、kind 取值、正文长度下限），
   任何一个用例非法时给出"文件名 + 字段 + 期望"的可执行错误信息后退出，不静默跳过。
2. 新增 evals/cases/README.md：告诉作者如何添加真实用例——字段说明、kind=idea 与
   kind=draft 各占一半的建议、总量 10–20 个、敏感内容脱敏提醒、示例 JSON。
3. 放行门判定（写进 ReportGenerator）：当结果集中同时存在 pipeline="agentic" 与
   pipeline="deep" 的数据时，报告末尾输出"放行门"三项判定与总结论：
   (a) agentic 的 rubric 平均总分 ≥ deep；
   (b) agentic 的平均模型调用次数 ≤ deep 的 1.5 倍；
   (c) agentic 的 fallback/熔断率 ≤ deep。
   三项全过 → "放行门：通过，可将代理会话设为默认入口"；任一不过 → 列出未过项与差值。
   暂无 agentic 数据时输出"放行门：暂无 agentic 管线数据"（agentic 管线由后续任务 14 接入，
   本任务只写判定逻辑，不实现管线）。
4. 每个用例的结果记录需包含：模型调用次数、fallback/熔断发生数（若现有结果结构缺这两个
   字段则补上，兼容旧数据）。

边界：只动 CreativeWorkshopEval 目标与 evals/ 目录及其测试；不改主 App 任何代码。

验收标准：
1. 单元测试：用合成结果集分别覆盖放行门三项各自"过/不过"与"无 agentic 数据"共 ≥4 条路径。
2. 单元测试：非法用例（缺字段/kind 非法）触发可执行错误信息。
3. evals/cases/README.md 存在且包含字段表与示例。
4. 全部测试与护栏通过。
```

### 任务 12：半自动调度——"按计划执行"（PRD 23.5，P1，L1.5）

```text
任务：智能下一步目前只展示 execution_plan / suggested_actions（22.2.3 遗留）。
增加"按计划执行"：作者批准一次，系统按序执行计划中的动作，全程可取消，
生成类产物照常走待复核。

背景（先核实）：
- WritingAdvisorResult.suggested_actions 已有 → AdvisorAction 的 compactMap 映射
  （WorkshopModels.swift 中 advisorActions 一类计算属性）。
- WorkshopStore.performAdvisorAction 目前对每个动作 fire-and-forget（Task { await ... }），
  且 run(_:) 是单飞行护卫：并发触发第二个操作会被拒绝。顺序执行必须逐个 await，
  不能沿用 fire-and-forget。
- 生成类动作（quickDraft / draftFromOutline / polishDraft 等）完成后会产生
  pendingDraftReview，且后续生成类动作在 pending 存在时会被 guard 拒绝——
  这是特性不是障碍：计划执行必须在此暂停。

实现要求：
1. 编排放独立类型（Core 内新增，如 AdvisorPlanRunner），Store 只暴露
   @Published 的执行进度状态与启动/取消入口（21.5 护栏）。
2. 可执行动作子集：generate_topics、generate_outline、draft_from_outline、quick_draft、
   writing_review、polish_natural、polish_tighten、publish_assets。
   计划中出现 rewrite_selection_*（需要选区）或 save_article（作者行为）时跳过该步，
   在进度里注明"已跳过：需要作者手动执行"。
3. 顺序语义：逐步 await 完成再进下一步；某步产生 pendingDraftReview → 序列暂停，
   作者"确认定稿" → 自动继续下一步；作者"放弃" → 整个序列停止，
   提示"计划已停止：你放弃了第 N 步的产物"。
4. 任一步骤失败（statusText 报错或抛错）→ 序列停止并给出失败原因，已完成步骤的产物保留。
5. 全程可取消（沿用现有取消机制）；执行期间"按计划执行"按钮不可再次触发。
6. 持久化：整个序列作为一条 agent_runs 记录，新增列 session_kind（本任务取值
   "plan_execution"，历史行默认空），每步落 agent_steps；迁移走 user_version。
7. UI：智能下一步卡片增加"按计划执行"按钮（计划为空或 isLoading 时禁用），
   执行中显示"第 N/M 步：动作名"。

验收标准（用假 AI client 的确定性测试）：
1. 三步计划按序执行且逐步 await（用调用顺序断言）。
2. 生成步产生 pending → 序列暂停；确认 → 自动继续；放弃 → 序列停止。
3. 中途失败 → 停止且保留已完成产物；取消 → 立即停止。
4. rewrite_selection_* 被跳过并注明。
5. agent_runs.session_kind 迁移测试（旧库升级后历史行可读）。
6. 全部测试与护栏通过。
```

### 任务 13：验证信号统一为 VerificationReport（PRD 23.7，P1，纯重组）

```text
任务：质量门（AgentDraftQualityGate）、生成后自检（DraftSelfCheckResult）、雷区命中、
发表前终审（PrePublishAuditReport）目前各自为政。抽一个统一的 VerificationReport 结构，
为任务 14 的决策环提供"眼睛"。纯重组，不新增任何检查，不改任何 prompt。

实现要求：
1. Core 新增 VerificationReport：条目 = {来源（gate/selfCheck/pitfall/audit）、
   等级（passed/review 或 高/中/低）、标题、detail}；提供两个渲染：
   summaryLine（一行总结）与 compactLines(limit: 6)（供决策上下文，最多 6 行，
   高严重度优先，超限时以"另有 N 项"收尾）。
2. 三个消费方：
   a. 待复核卡片：现有 UI 改读 VerificationReport，展示内容与改造前逐项一致（不减项、不变形）；
   b. eval 报告：每条结果增加验证信号列（来自 summaryLine）；
   c. 决策上下文：暂只暴露 compactLines API，由任务 14 消费。
3. 构造入口集中一处（如 VerificationReport.build(gate:selfCheck:pitfalls:audit:)），
   各来源均为可选参数，缺哪个就不出现哪个来源的条目。

验收标准：
1. 单元测试：四种来源各自的映射正确；compactLines 超过 6 项时截断且高严重度排前。
2. 存量 UI 回归：构造与改造前相同的输入状态，断言待复核卡片读到的条目集合不变
   （store 层测试即可，不要求 UI 快照）。
3. 全部测试与护栏通过。
```

### 任务 14：有界代理循环内核 WritingAgentCoordinator（PRD 23.6，P1，本批核心）

```text
任务：实现 L2 有界代理循环的内核：模型观察会话状态 → 在封闭动作空间选下一步 →
护栏执行与验证 → 结果回流，直到 finish / ask_author / 预算耗尽 / 熔断。
本任务只做内核 + 决策工作流 + 持久化 + eval 接入 + 测试，不做 UI（任务 15）。

先读透一个关键架构决定（做错会死锁）：
- Store 的生成类动作函数（quickDraft / polishDraft / improveDraftFromLatestReview…）
  每次都会产生 pendingDraftReview，且 pending 存在时后续生成动作被 guard 拒绝。
  因此 L2 会话内部【绝不能】调用 Store 的动作函数——第一个生成动作后就会死锁。
- 正确做法：WritingAgentCoordinator 在【会话本地状态】上直接消费
  NativeWorkflowCatalog 的 descriptor 与既有协调器（AgentDraftCoordinator 等），
  中间产物只更新会话内的 currentBest 草稿快照并入轨迹；
  仅在会话结束时把最终稿一次性交付 pendingDraftReview
  （before = 会话开始前的编辑器快照，after = 最终稿）。
  这正是"半自动调度（任务 12，走 Store + 逐步待复核）"与"L2（本任务，会话内自治 +
  终点待复核）"的本质区别。
- 会话内的写作诊断参照 DeepDraftCoordinator 的先例用 transient review，
  不写入 writing_reviews（不污染写作教练历史）。

实现要求：
1. 【决策工作流】AIWorkflowKind 新增 case agentDecision = "agent_decision"，
   归入 defaultParameters 的判别类分支（0.2 / 4096 / 120s）。
   NativeWorkflowCatalog 新增 agentDecision descriptor；NativePrompts 新增对应 builder，
   prompt 文案【逐字】使用 创作工坊-PRD.md 23.6.3 代码块的内容（含决策纪律五条与
   JSON 输出格式），system prompt 为："你是严谨的中文写作代理调度器，只输出用户要求的 JSON。"
   appendAgentDiscipline 新增 decision 模式，聚焦文案：
   "决策调度：每次只选一个动作；证据与验证信号优先；在预算内行动；把不确定交还作者永远是合法选择。"
   模板种子照常加入 defaultPromptTemplates。
2. 【会话状态】AgentSessionState：写作目标（想法/方向/选题）、稿件度量（阶段/字数/大纲有无）、
   最新 VerificationReport 的 compactLines（任务 13）、动作历史（每条一行：
   "动作 → 一句话结果 → 信号变化"）、剩余预算。观察压缩铁律（设计律 a）：
   正文只给首尾节选（沿用 WritingContextBuilder.excerptContent 思路），
   完整产物只入 agent_steps，不入决策上下文。
3. 【动作空间】封闭枚举 + 前置条件（护栏在每轮决策前按状态生成"可用动作清单"注入 prompt，
   并在模型返回后二次校验）：
   - generate_outline（前置：大纲为空）
   - draft_from_outline（前置：大纲非空）
   - agent_quick_draft（前置：正文字数 < 300；复用 AgentDraftCoordinator）
   - writing_review（前置：存在可诊断文本；transient，不落 writing_reviews）
   - improve_from_review（前置：会话内存在最近诊断且 issues 非空）
   - polish_natural / polish_tighten（前置：正文非空）
   - ask_author {questions}（前置：本会话未用过；每会话至多 1 次）
   - finish（恒可用）
   本任务不含 search_materials（任务 16 加入）。
4. 【预算与熔断】
   - 每会话模型调用上限：settings 键 agent_call_budget，默认 12，决策步计入；
   - 剩余 ≤2 时可用动作收缩为 {writing_review, ask_author, finish}（清单生成 + 校验双重强制）；
   - 同一动作连续 >2 次 → 强制停机；
   - 动作执行后状态哈希（标题 + 正文 + 验证信号摘要）不变 → 记一次空转，累计 2 次 → 停机；
   - 决策 JSON 非法或动作越界/违反前置 → 给一次纠正观察让模型重选（解析层重试已由
     22 章任务 3 覆盖），仍违规 → 安全停机；
   - 任何一步触发熔断语义（非 missingAPIKey 的失败）→ 会话结束；
   - missingAPIKey：会话入口直接不可用并提示配置 Key，不做假演示（PRD 23.6.4 的有意例外）；
   - 一切停机路径都交付当前最好版本进 pendingDraftReview，不空手（设计律 e）。
5. 【交付】会话结束（finish 或任何停机）→ 最终稿走一次 finalizeGeneratedDraft 语义进入
   pendingDraftReview，PendingDraftReview 增加可选的会话摘要字段：总步数、
   每步"动作 + 理由"一行、验证信号首尾对比、停止原因。
   ask_author → 会话输出为"提问"结果：具体缺口清单 + 会话可续跑句柄（剩余预算、状态快照），
   Store 以 @Published 暴露（UI 在任务 15）。
6. 【持久化】agent_steps 新增列 step_type（decision / action / verification，历史行默认
   action）与 decision_json（决策原文）；agent_runs 增 budget_summary；
   session_kind 取值 "agent_session"（列已由任务 12 建）。迁移走 user_version + 迁移测试。
7. 【eval 接入】PipelineRunner 新增 pipeline "agentic"：无头跑 WritingAgentCoordinator
   （注入 executor 与临时库），记录 rubric 评分、调用次数、熔断数——任务 11 的放行门
   从此有数据。eval 中 ask_author 视为 finish 处理（无人可问）。
8. 【护栏】architecture_guard.sh 增加一条：Sources/CreativeWorkshopCore/Services 下的
   *Coordinator*.swift 不得出现 "ChatMessage(" 构造（prompt 只能在 NativePrompts / 模板）。
9. 【入口守卫】settings 键 agent_lab_enabled 默认 false；Store 的会话启动入口在开关关闭时
   直接拒绝（UI 在任务 15）。

验收标准（全部用假 executor 的确定性测试，逐条对照 PRD 23.6 验收）：
1. 预算耗尽 → 交付当前最好版本，budget_summary 记录用量；
2. 同一动作连续 3 次 → 强制停机；两次空转 → 停机；
3. 决策返回未列入清单的动作 → 一次纠正观察 → 仍违规 → 停机交付；
4. 决策 JSON 非法 → 安全停机路径覆盖；
5. ask_author → 输出提问结果且可用句柄续跑（预算继承）；每会话第 2 次 ask_author 被拒；
6. 每个决策步以 step_type="decision" 落库且 decision_json 可读；迁移测试覆盖新列；
7. missingAPIKey 时会话入口报错，不进入循环；
8. agent_lab_enabled=false 时入口拒绝，系统其余行为与实施前完全一致；
9. eval 跑 agentic 管线产出结果且报告出现放行门判定；
10. 全部测试与护栏通过（含新增护栏条目自测：故意在协调器写 ChatMessage( 应被脚本拦截，
    验证后移除）。
```

### 任务 15：代理会话 UI（PRD 23.6.5 / 23.6.6，P1）

```text
任务：为任务 14 的内核补 UI。实验室开关、会话入口、执行中状态、会话摘要、
代理提问卡与续跑。视图层不得直接引用 AI 组件（护栏既有规则）。

实现要求：
1. 设置页新增"实验室"区：agent_lab_enabled 开关 + 一句说明
   （"代理会话：模型在预算内自主决定写作步骤，产物仍需你确认。评测放行前默认关闭。"）。
2. 开关开启时，工作区出现"代理会话"入口按钮（isLoading 或 pending 存在时禁用）；
   开关关闭时该入口不可见，且所有既有界面与行为与任务 14 之前完全一致。
3. 执行中：沿用流式状态条显示"第 N 步 · 动作名"，可随时取消（沿用现有取消机制）。
4. 会话结束：待复核卡片展示会话摘要（总步数、每步动作 + 理由一行、验证信号首尾对比、
   停止原因）——读任务 14 已有的摘要字段，不新造数据。
5. ask_author：出现"代理提问"卡，逐条列出具体缺口；作者补充素材/输入后点"继续会话"，
   用任务 14 的续跑句柄继续（预算继承）；也可"就此结束"，按 finish 语义交付。
6. UI 文案克制：不用"AI 全自动"之类的表述，统一用"代理会话（实验室）"。

验收标准：
1. store 层测试：开关关闭时入口守卫拒绝；提问卡状态 → 续跑 → 交付的状态流转正确。
2. 手动验证清单（报告里逐条确认）：开关开/关的入口可见性；执行中取消；
   会话摘要在待复核卡片完整可读；继续会话后最终交付正常。
3. 全部测试与护栏通过（视图层引用检查不新增违规）。
```

### 任务 16：search_materials 动作 + 检索质量升级（PRD 23.8.1，P2）

```text
任务：把本地素材检索从"代码在固定时机注入"升级为"模型发现证据缺口时主动调用的动作"，
同批落地第 22 章遗留的检索精度升级。本地零网络。

实现要求：
1. 检索升级（FragmentRetriever，纯函数，先做这半，独立可测）：
   a. 中文停用词表（的/了/是/我们/一个 等常用虚词，内置数组即可）在关键词抽取时过滤；
   b. IDF 加权：对语料库内的 token/bigram 计算逆文档频率，检索得分从纯 Jaccard 改为
      IDF 加权重叠（保留现有 title/content 命中加成）；
   c. 命中率记录：检索调用的 query、返回数、最高分落入所属 agent_steps 的 output_summary。
2. 动作接入（任务 14 的动作空间扩充）：
   - 新增 search_materials {query}：前置条件——本会话该 query 未检索过（避免重复检索空转）；
   - 执行：FragmentRetriever top-3 → 结果压缩为"来源 + 首行"进入下一轮观察；
     完整片段入 agent_steps；命中片段计入会话交付时的引用轨迹（复用现有
     retrievedFragments 展示）；
   - 决策 prompt 的可用动作清单同步补该动作说明（护栏生成，不改 23.6.3 模板正文）；
   - 检索为纯本地操作，不计入模型调用预算，但每会话上限 4 次（防检索空转）。
3. 评测联动：eval 的 agentic 管线自动获得该动作；报告的调用次数统计不包含检索
   （它不是模型调用），但记录检索次数列。

验收标准：
1. 检索单元测试：固定语料下，停用词过滤与 IDF 加权改变排序的用例 ≥2 条
   （构造"高频词命中 vs 低频关键词命中"的对照）；
2. 会话测试（假 executor）：模型发起 search_materials → 结果进入下一轮观察；
   同 query 第二次被拒；第 5 次检索被拒；
3. 交付的待复核卡片能看到本次会话引用的素材片段；
4. 全部测试与护栏通过。
```

### 任务 17（条件任务）：write_section 逐节写作动作（PRD 23.8.2，P2，评测驱动）

```text
任务（先判定，再实施）：本任务有前置证据条件，不满足就停止。

第一步——判定（必做）：
读取 evals/reports/ 最新一份对照报告，回答："整篇一次生成"是否仍是 agentic/deep 管线
的主要失分点（长文中段质量、结构偏差、截断类信号占失分的主导地位）？
- 如果报告数据不足或失分点不在此 → 输出判定依据，停止，不写任何代码；
- 如果条件成立 → 输出判定依据后继续第二步。

第二步——实施：
1. 动作空间新增 write_section {index}：按 brief/大纲的结构计划逐节生成
   （复用 sectionDraft 的 prompt 与素材按段检索机制，改造为单节粒度），
   每节完成后立即跑本地验证器，结果进入下一轮观察；
2. 前置条件：存在结构计划且 index 为未完成节；全部节完成后该动作不可用；
3. 会话状态增加"各节完成度"视图供决策观察；
4. 预算语义不变（每节生成是一次模型调用）。

验收标准：
1. 判定过程与依据写进任务报告（无论是否实施）；
2. 若实施：假 executor 测试覆盖逐节推进、节验证信号回流、全节完成后动作下架；
3. 全部测试与护栏通过。
```

### 任务 18：调度偏好记忆（PRD 23.8.3，P2，最后做——需要会话数据积累）

```text
任务：会话结束后做轻量复盘，归纳"哪类动作序列对这位作者收益高"，
经作者确认后注入决策 prompt。机制完全复用雷区/编辑偏好的
"归纳自动、生效必须人工确认"闭环，不发明新模式。

实现要求：
1. 数据源：近期 agent_runs（session_kind="agent_session"）的步骤序列、
   每步验证信号变化、最终是否被作者确认定稿；
2. 新增 schedulingPreferenceSummary 工作流（判别类），从数据源归纳候选
   "调度偏好规则"（例："这位作者的稿子先跑一轮诊断再改，比直接润色收益更大"），
   每条附支持它的 agent_run id，最多 6 条；prompt 结构仿照 summarizeEditPreferences；
3. 存储：StyleProfile 新增 scheduling_preferences JSON 字段（结构同 learned_preferences），
   候选经 Settings 确认后写入，迁移走 user_version；
4. 注入：已确认规则由护栏拼入 agentDecision 的观察区（新增一个"调度偏好"小节，
   不改 23.6.3 模板正文），未确认的候选绝不注入；
5. 会话样本 < 5 条时"归纳调度偏好"入口禁用并提示样本不足。

验收标准：
1. 解码/迁移测试覆盖新字段；候选未确认不注入（prompt 构造测试断言）；
2. 样本不足时入口禁用；
3. 全部测试与护栏通过。
```

### 任务 19（文档，最后做）：第 23 章状态回填

```text
任务：只改文档，不改代码。
1. 创作工坊-PRD.md 第 23 章：状态行改为已实施（写明日期与核验结果），
   23.4–23.8 各节按第 18/22 章惯例追加"实施记录"（如实记录与提案的差异，
   包括任务 17 的判定结论——实施了还是证据不足未实施）；
2. 若放行门已判定：在 23.6.6 记录 L2 是否转为默认入口及依据（报告文件名与三项数值）；
3. ARCHITECTURE.md 组件表补 WritingAgentCoordinator、CreativeWorkshopEval、
   实验室开关一行说明；
4. PRD_完成度审计.md 若有涉及本章范围的表述，同步更新；
5. 不改动任何未提及章节的文字。
```

---

## D. 使用建议

1. **顺序执行**：11 → 12 → 13 → 14 → 15，然后**停下来跑评测**（真实用例 + `swift run --package-path macos/CreativeWorkshopMac CreativeWorkshopEval --pipelines direct,agent,deep,agentic`），看放行门结论，再决定 16/17 的走向；18、19 收尾。
2. **有一件事只有你能做**：任务 11 之后、任务 14 之前，把 `evals/cases/` 的占位用例换成你的 10–20 个真实历史输入。编码代理不能也不应编造你的个人写作数据——放行门的判定质量完全取决于用例的真实度。
3. **任务 17 是条件任务**：它的提示词第一步就是读评测报告做判定，证据不足时代理会拒绝实施——这是刻意设计，别催它写代码。
4. **风险都关在两道门里**：所有新能力藏在"实验室"开关后（默认关），所有产物仍过待复核。任何时候觉得不对，关开关即回到现状。
5. 每个任务一个新会话：粘贴 B 节通用约束块 + 对应任务提示词即可，任务之间你只审 diff 和验证报告。
