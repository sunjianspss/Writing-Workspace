# 创作工坊 · 产品需求文档（修订版）

整理：参宿ag

---

> **修订说明**：本文档在原版 PRD 基础上，针对五个核心问题进行了修订：
> 1. **风格库**：从「文字描述」改为「Few-shot 样本」驱动
> 2. **每日流程**：新增「一键直出」模式，将 10 步压缩到 2 步
> 3. **Prompt 上下文管理**：补充各环节的输入来源设计
> 4. **复盘分析**：第一版降为 P1；如实现，只依赖工具内部数据
> 5. **开发优先级**：风格库基础配置提前至第 1 天
> 6. **MVP 范围收敛**：第一版先完成「想法 → 初稿 → 编辑 → 发布物料 → 归档/导出」最短闭环，素材箱、选题池、复盘分析降为 P1

---

## 1. 产品概述

**产品名称**：创作工坊

**产品定位**：面向公众号日更作者的 AI 写作工作台。不是「AI 代写工具」，而是帮助作者完成从灵感记录、选题生成、文章大纲、正文初稿、风格润色、标题优化、发布物料生成到文章归档复盘的完整写作流程。

**一句话介绍**：每天输入一个想法，20 分钟内生成一篇符合个人风格、可继续修改、可直接发布的公众号文章。

**产品目标**：

1. 降低每天公众号写作的启动难度
2. 帮助作者把零散灵感整理成完整文章
3. 保持文章风格统一，减少 AI 味
4. 沉淀个人素材库、选题库、文章库
5. 后续可扩展为面向其他创作者的写作产品

---

## 2. 用户画像

**第一版目标用户**：产品只服务一个用户——工具开发者本人。

用户特征：几乎每天写公众号；经常有碎片化灵感但不一定能马上写成文章；需要 AI 辅助提效；希望文章保留个人表达；有一定技术能力可本地部署。

**用户痛点**：

1. 每天不知道写什么
2. 有想法，但展开困难
3. 空白文档带来启动压力
4. AI 直接生成的文章太像模板文
5. 标题、摘要、朋友圈文案需要额外花时间
6. 过去写过的文章和素材没有形成资产

---

## 3. 使用场景

### 3.1 每日写作场景——两种入口并存

> **修订**：原版只有「步步推进」一种路径（10 步）。修订版新增「一键直出」作为默认入口，步步推进改为可选的精细控制模式。

**模式 A：一键直出（日常默认）**

用户打开工具，输入粗略想法，点「直接生成初稿」。系统跳过选题池和大纲确认，直接输出完整初稿。全程 1～2 步完成。

```
示例：
输入：「普通人如何用 DeepSeek API 做个人工具」
点击：直接生成初稿
输出：约 1500 字的完整初稿（含标题建议）
后续：进编辑器修改 → 生成发布物料 → 归档
```

**模式 B：步步推进（精细控制）**

需要精细把控文章方向时可选：输入想法 → 选题池 → 大纲确认 → 初稿生成 → 润色 → 发布物料。

### 3.2 灵感记录场景

随时记录碎片灵感到素材箱，生成文章时可引用。

### 3.3 风格润色场景

已有草稿，粘贴原文，选择润色目标，系统输出优化版本。

### 3.4 发布准备场景

正文完成后，一键生成：摘要、封面文案、朋友圈转发文案、标签。

### 3.5 文章归档场景

发布后保存文章并记录数据，供后续复盘。

---

## 4. 产品范围

### 4.1 第一版 MVP 必须做（P0）

- 今日写作工作台（含一键直出模式）
- 个人风格库基础配置（默认风格 + 粘贴 / 上传 1 篇历史文章作为 Few-shot 样本，**第 1～2 天完成并接入生成链路**）
- 初稿生成
- 风格润色
- 标题生成
- 发布物料生成（核心 4 项：摘要 / 封面文案 / 朋友圈文案 / 标签）
- 文章保存归档 / 导出 Markdown
- DeepSeek API 配置

**P0 最短闭环**：

```
配置 API Key → 配置默认风格 / 样本 → 输入想法 → 一键生成初稿
→ 编辑润色 → 生成发布物料 → 保存归档 / 导出 Markdown
```

### 4.2 第一版可选做（P1）

- 素材箱基础 CRUD
- 从素材生成选题
- 选题池
- 大纲生成（步步推进模式）
- 内部数据统计
- AI 复盘

### 4.3 第一版暂不做

- 多用户登录 / 付费系统 / 团队协作
- 微信公众号自动发布 / 自动抓取热点 / 自动读取后台数据
- 移动端 App / 浏览器插件
- 复杂知识库检索 / AI 图片生成
- 小红书版本 / 短视频口播文案（延至第二版）
- 基于外部数据的复盘分析（阅读量 / 转发量等外部数据维度延至第二版）

---

## 5. 信息架构

产品第一版 MVP 包含 4 个 P0 页面，P1 功能可在同一信息架构中后续补齐。

1. 首页 / 今日写作工作台
2. 写作编辑器
3. 文章归档
4. 设置页

P1 页面：

1. 素材箱
2. 选题池

---

## 6. 功能需求

### 6.1 首页 / 今日写作工作台

页面内容：
- 今日日期 + 本周写作计数
- 今日写作输入框
- 写作方向选择（标签）
- 两个核心按钮：**「直接生成初稿」**（默认大按钮）+ 「步步推进」（次要按钮）
- 最近素材 / 最近文章 / 待写选题

写作方向选项：AI 工具 / 技术感悟 / 个人成长 / 情感文学 / 社会观察 / 工作复盘 / 自定义

> **修订**：一键直出模式下，系统在单次 API 调用中完成「核心观点推导 → 结构规划 → 正文生成」，不拆分为多步。Prompt 见第 11.1 节。

### 6.2 素材箱

**优先级：P1。** MVP 可先不做完整素材箱，只在一键直出时支持用户手动粘贴「可用素材」。

> **修订**：原版 9 种类型改为 3 种基础类型 + 标签扩展，降低新增素材时的分类摩擦。

**素材类型**：灵感 / 金句 / 文章片段（通过标签自由扩展）

**素材字段**：id / title / content / type / tags / used / related_article_id / created_at / updated_at

**核心操作**：新增 / 编辑 / 删除 / 搜索 / 标签筛选 / 一键加入今日写作 / 从素材生成选题 / 扩展成段落

**AI 辅助**：扩写成公众号段落 / 改写成更有情绪的表达 / 生成 5 个相关选题

### 6.3 选题池

**优先级：P1。** MVP 可先不做独立选题池页面，只保留一键直出和编辑器内的标题生成能力。

生成方式：根据用户输入生成 / 根据素材箱生成（P1 优先实现这两种）

每个选题包含：标题 / 一句话说明 / 推荐角度 / 情绪强度 / 推荐指数

选题状态：待写 / 写作中 / 已完成 / 已放弃

### 6.4 写作编辑器

**布局**：左侧文章编辑区（标题 / 正文 / 摘要 / 标签 / 保存按钮），右侧 AI 操作面板。

**AI 操作面板**：生成大纲 / 生成初稿 / 续写 / 润色 / 改开头 / 改结尾 / 减少 AI 味 / 生成标题 / 生成发布物料

**润色选项**：优化行文流畅度 / 增强情绪感染力 / 改得更像我 / 减少 AI 味 / 更通俗 / 更克制 / 更有文学感 / 更适合公众号 / 只改错别字和病句 / 保持原意重新表达

**局部修改**：选中一段文字可执行：扩写 / 缩短 / 改得更自然 / 增强画面感 / 改成口语化 / 改成深刻一点 / 生成 3 个替代版本

### 6.5 个人风格库

> **修订（核心）**：风格库不只是文字描述，而是「样本文章 + 描述标签」双轨驱动。每次生成文章时，将 1～2 篇样本文章以 Few-shot 格式传入 Prompt，比单纯文字描述效果好得多。

**默认风格设定**：

```
语言：中文，通俗易懂，有个人表达感
语气：真诚、克制、略带感慨，但不鸡汤
结构：从现实痛点切入，再讲观察，再给方法或感悟
句式：短句和中等句结合，避免长篇大论
禁忌：不要标题党，不要培训腔，不要营销腔，不要堆砌概念
目标：像一个真实的人在写，而不是 AI 在生成
```

**风格字段**：

```
id / name / language_style / tone / structure_preference
favorite_expressions / forbidden_expressions
sample_texts          ← 核心新增：存 2～3 篇历史文章全文，用于 Few-shot
title_style_like / title_style_dislike
is_default / created_at / updated_at
```

**P0 风格操作**：编辑默认风格 / 设置默认风格 / 粘贴或上传 1 篇历史文章作为风格样本 / 在一键直出中生效

**P1 风格操作**：新增 / 删除多个风格 / 上传多篇历史文章 / 对比不同风格生成效果

**第一版实现方式**：不需要训练或微调模型。每次生成文章时，Prompt 结构如下：

```
系统角色：你是一个公众号写作助手，下面是这位作者的历史文章样本，
          请学习其语气、结构和表达习惯。

样本文章 1：
{sample_text_1}

样本文章 2（如有）：
{sample_text_2}

风格描述补充：
{style_description}

现在请根据以下选题生成一篇类似风格的文章：
{topic} / {outline} / {materials}
```

### 6.6 标题生成

每次生成 10 个标题，涵盖：情绪型 / 工具型 / 反差型 / 思考型 / 克制型 / 文学型

每个标题包含：标题文本 / 标题类型 / 推荐理由 / 风险提示

原则：不标题党 / 不夸大 / 不低俗 / 能承接正文 / 有点击欲望但不空洞

### 6.7 发布物料生成

> **修订**：原版 8 种物料精简为 4 种核心 + 2 种可选。

**核心物料（每次必出）**：
- 公众号摘要（50～120 字）
- 公众号封面文案（20 字以内）
- 朋友圈转发文案（100 字以内，像作者自己写的推荐语）
- 文章标签（5～8 个）

**可选物料（按需生成）**：小红书版本 / 封面图提示词

### 6.8 文章归档

**文章字段**：id / title / content / summary / status / type / tags / published_at / read_count / like_count / wow_count / share_count / comment_count / self_score / related_material_ids / related_topic_id / created_at / updated_at

**文章状态**：草稿 / 已完成 / 已发布 / 已归档 / 可二次改写

**归档操作**：保存 / 编辑 / 删除 / 搜索 / 标签筛选 / 时间筛选 / 复制内容 / 记录发布数据

### 6.9 复盘分析

**优先级：P1。** MVP 只记录文章和自评分，不强制实现统计看板与 AI 复盘。

> **修订（核心）**：原版复盘依赖手动录入的公众号后台数据，实际使用中几乎不会录入。修订版改为只统计工具内部数据。

**内部数据统计（第一版）**：
- 本周 / 本月生成文章数 + 发布文章数
- 素材箱总量 / 已使用 / 未使用
- 选题池待写数量
- 使用最多的写作方向
- 连续写作天数

**AI 复盘**：用户选中某篇文章点击「AI 复盘」，输出：优点 / 问题 / 标题是否合适 / 开头是否吸引人 / 结构是否清晰 / 下次如何改进 / 是否适合二次改写

**外部数据（第二版）**：等第二版再实现阅读量 / 转发量维度分析，或接入自动同步能力。

### 6.10 设置页

- DeepSeek API Key / API Base URL / 默认模型
- 是否开启流式输出
- 默认写作风格 / 默认文章长度 / 默认标题数量
- 数据库路径 / 导出数据 / 导入数据

安全要求：API Key 存储在 `.env` 文件，前端不直接调用 DeepSeek，由后端代理。

---

## 7. 非功能需求

| 类型 | 要求 |
|------|------|
| 性能 | 页面打开 < 3秒；AI 请求 < 30秒；长文章支持流式输出；保存/读取 < 1秒 |
| 可用性 | 操作路径短；按钮命名直观；生成结果可一键复制；用户不需要理解 Prompt |
| 可靠性 | API 失败提示原因；支持重试；编辑时自动保存草稿；避免误删 |
| 安全性 | API Key 不暴露给前端；用户数据默认保存本地；不上传至第三方 |

---

## 8. 技术方案

### 8.1 技术栈

| 层次   | 选型                                                  |
| ---- | --------------------------------------------------- |
| 前端   | Next.js / React / Tailwind CSS                      |
| 后端   | Python FastAPI                                      |
| 数据库  | SQLite                                              |
| 模型服务 | DeepSeek API（`deepseek-v4-flash`/`deepseek-v4-pro`） |
| 运行方式 | 本地运行，后续可部署到云服务器                                     |

### 8.2 上下文管理设计

> **修订**：原版 Prompt 设计未说明各步骤的输入来源，实现时容易踩坑。修订版明确每个 Prompt 的数据来源。

| 生成环节 | 输入来源 |
|----------|----------|
| 一键直出初稿 | 前端输入框 + DB: 默认风格（含样本）+ 用户手动粘贴素材（可选） |
| 选题生成 | 前端输入 + DB: 风格描述 |
| 大纲生成 | session: 已选选题 + DB: 风格 + DB: 用户选入的素材 |
| 初稿生成 | session: 大纲内容 + DB: 风格（含 Few-shot 样本）+ DB: 选入素材 |
| 润色 | 编辑器当前文章内容 + DB: 风格 + 用户选择的润色目标 |
| 发布物料 | 编辑器当前标题 + 正文内容 |

**上下文选择规则（P0）**：

1. 风格样本优先级：用户当前选择的默认风格样本 > 默认风格描述。
2. P0 最多传入 1 篇样本文章；P1 支持 2～3 篇样本文章。
3. 单篇样本文章过长时，后端截取「开头 800 字 + 中间 800 字 + 结尾 800 字」，避免 Prompt 过长。
4. 一键直出时的素材来源优先级：用户手动粘贴素材 > 用户选中的素材（P1）> 同方向最近未使用素材（P1）。
5. P0 不自动塞入大量最近素材，避免生成内容跑偏；只有用户明确提供素材时才进入 Prompt。
6. 所有 AI 接口需要记录本次调用使用的 `style_profile_id`、`material_ids`（如有）、模型名和耗时，便于后续复盘。

**长文章处理**：正文超过约 2000 字时，润色操作改为分段处理（前端切段，逐段调用 API，后端合并）。

### 8.3 请求流程

1. 前端提交选题、素材、风格参数
2. 后端从 DB 读取风格设定（含样本文章）
3. 后端拼装 Prompt（Few-shot 格式）
4. 后端调用 DeepSeek API（流式输出）
5. 前端实时展示生成结果
6. 用户编辑或保存文章

### 8.4 AI 输出解析规则

为了降低前端解析成本，核心生成接口默认要求模型输出 JSON。若模型返回非 JSON，后端保留原文并返回 `raw_output`，前端展示「解析失败，可复制原始结果」。

核心接口返回结构：

```json
{
  "title": "文章标题",
  "content": "正文 Markdown",
  "summary": "摘要",
  "tags": ["标签1", "标签2"],
  "raw_output": null
}
```

标题生成返回结构：

```json
{
  "titles": [
    {
      "text": "标题文本",
      "type": "思考型",
      "reason": "推荐理由",
      "risk": "风险提示"
    }
  ]
}
```

发布物料返回结构：

```json
{
  "summary": "公众号摘要",
  "cover_text": "封面文案",
  "moments_text": "朋友圈转发文案",
  "tags": ["标签1", "标签2"]
}
```

---

## 9. 数据库设计

### 9.1 ideas 素材表

```sql
id                  INTEGER PRIMARY KEY
title               TEXT
content             TEXT NOT NULL
type                TEXT        -- 灵感 / 金句 / 文章片段
tags                TEXT        -- JSON 数组
used                BOOLEAN DEFAULT 0
related_article_id  INTEGER
created_at          DATETIME
updated_at          DATETIME
```

### 9.2 topics 选题表

```sql
id              INTEGER PRIMARY KEY
title           TEXT NOT NULL
direction       TEXT
core_viewpoint  TEXT
target_reader   TEXT
status          TEXT        -- 待写 / 写作中 / 已完成 / 已放弃
tags            TEXT
created_at      DATETIME
updated_at      DATETIME
```

### 9.3 articles 文章表

```sql
id                    INTEGER PRIMARY KEY
title                 TEXT
content               TEXT
summary               TEXT
status                TEXT        -- 草稿 / 已完成 / 已发布 / 已归档 / 可二次改写
type                  TEXT
tags                  TEXT
published_at          DATETIME
read_count            INTEGER DEFAULT 0
like_count            INTEGER DEFAULT 0
wow_count             INTEGER DEFAULT 0
share_count           INTEGER DEFAULT 0
comment_count         INTEGER DEFAULT 0
self_score            INTEGER
related_material_ids  TEXT        -- JSON 数组
related_topic_id      INTEGER
created_at            DATETIME
updated_at            DATETIME
```

### 9.4 style_profiles 风格表

```sql
id                    INTEGER PRIMARY KEY
name                  TEXT NOT NULL
language_style        TEXT
tone                  TEXT
structure_preference  TEXT
favorite_expressions  TEXT
forbidden_expressions TEXT
sample_texts          TEXT        -- JSON 数组，存 2～3 篇历史文章全文（Few-shot 核心）
title_style_like      TEXT
title_style_dislike   TEXT
is_default            BOOLEAN DEFAULT 0
created_at            DATETIME
updated_at            DATETIME
```

### 9.5 prompts 提示词模板表

```sql
id          INTEGER PRIMARY KEY
name        TEXT
type        TEXT    -- 选题/大纲/初稿/润色/标题/发布物料/一键直出
content     TEXT
created_at  DATETIME
updated_at  DATETIME
```

### 9.6 settings 设置表

```sql
id          INTEGER PRIMARY KEY
key         TEXT UNIQUE
value       TEXT
created_at  DATETIME
updated_at  DATETIME
```

---

## 10. API 接口设计

```
# 风格（P0）
GET    /api/styles                  获取风格列表
POST   /api/styles                  新增风格
GET    /api/styles/default          获取默认风格
PUT    /api/styles/{id}             更新风格
POST   /api/styles/{id}/default     设置默认风格
POST   /api/styles/{id}/sample      上传或粘贴风格样本

# 素材（P1）
POST   /api/ideas                  新增素材
GET    /api/ideas                  获取素材列表
GET    /api/ideas/{id}             获取素材详情
PUT    /api/ideas/{id}             更新素材
DELETE /api/ideas/{id}             删除素材
POST   /api/ideas/{id}/expand      素材扩写成段落
POST   /api/ideas/{id}/topics      根据素材生成选题

# 选题（P1）
POST   /api/topics/generate        生成选题
GET    /api/topics                 获取选题列表
POST   /api/topics                 保存选题
PUT    /api/topics/{id}            更新选题
DELETE /api/topics/{id}            删除选题

# 写作
POST   /api/write/quick-draft      一键直出初稿（新增）
POST   /api/write/viewpoints       生成核心观点
POST   /api/write/outline          生成文章大纲
POST   /api/write/draft            生成文章初稿
POST   /api/write/polish           润色文章
POST   /api/write/title            生成标题
POST   /api/write/publish-assets   生成发布物料

# 文章
POST   /api/articles               保存文章
GET    /api/articles               获取文章列表
GET    /api/articles/{id}          获取文章详情
PUT    /api/articles/{id}          更新文章
DELETE /api/articles/{id}          删除文章
GET    /api/articles/{id}/export   导出 Markdown
POST   /api/articles/{id}/review   AI 复盘文章（P1）

# 统计（P1）
GET    /api/stats/overview         内部数据概览

# 设置
GET    /api/settings               获取设置
PUT    /api/settings               更新设置
POST   /api/settings/test-api      测试 DeepSeek API 连通性
```

---

## 11. Prompt 模板设计

### 11.1 一键直出 Prompt（新增）

> 这是修订版新增的核心 Prompt，完成选题 → 结构 → 正文的单次调用。

```
你是一个公众号写作助手。下面是这位作者的历史文章样本，
请仔细学习其语气、句式习惯和结构偏好：

【样本文章 1】
{sample_text_1_truncated}

【样本文章 2（P1，如有）】
{sample_text_2_truncated}

【风格补充说明】
{style_description}

现在请根据以下输入，直接生成一篇完整的公众号文章：

作者今天的想法：{input}
写作方向：{direction}
可用素材（如有）：{materials}

要求：
1. 从现实痛点或个人经历切入
2. 结构清晰，段落不要太长
3. 语言自然，不要排比句堆砌
4. 结尾要有余味，不要喊口号
5. 风格尽量接近样本文章
6. 正文使用 Markdown
7. 不要输出「好的，我来帮你生成」类前缀

请只输出 JSON，不要输出 Markdown 代码块：

{
  "title": "标题建议",
  "content": "完整正文 Markdown",
  "summary": "80 字以内摘要",
  "tags": ["标签1", "标签2", "标签3"]
}
```

### 11.2 生成选题 Prompt

```
你是我的公众号选题策划助手。
我的写作方向：{direction}
今天的关键词或想法：{input}
我的写作风格：{style_description}

请生成 10 个公众号选题。
要求：标题自然不标题党；适合公众号读者；有真实痛点；有个人表达空间。

请只输出 JSON，不要输出 Markdown 代码块：

{
  "topics": [
    {
      "title": "标题",
      "description": "一句话说明",
      "angle": "推荐角度",
      "score": 4
    }
  ]
}
```

### 11.3 生成大纲 Prompt

```
你是我的公众号文章大纲助手。
文章选题：{topic}
核心观点：{viewpoint}
可用素材：{materials}
我的写作风格：{style_description}

请生成文章大纲。
要求：开头从现实痛点切入；层次清晰；标注可插入素材位置；结尾有余味不喊口号。

请只输出 JSON，不要输出 Markdown 代码块：

{
  "title": "标题建议",
  "opening": "开头设计",
  "sections": [
    {
      "heading": "第一部分",
      "points": ["要点1", "要点2"],
      "material_hint": "可插入素材"
    }
  ],
  "ending": "结尾设计"
}
```

### 11.4 生成初稿 Prompt

```
你是我的公众号写作助手。

【作者风格样本】
{sample_text_1}

【风格说明】
{style_description}

文章选题：{topic}
文章大纲：{outline}
可用素材：{materials}

写作要求：语言通俗自然；像真实作者写的；段落不要太长；
不要过度排比；不要堆砌概念；开头能让读者读下去；结尾有回味。

请只输出 JSON，不要输出 Markdown 代码块：

{
  "title": "标题建议",
  "content": "完整正文 Markdown",
  "summary": "80 字以内摘要",
  "tags": ["标签1", "标签2", "标签3"]
}
```

### 11.5 润色 Prompt

```
你是我的公众号文章编辑。
原文：{content}
我的写作风格：{style_description}
润色目标：{polish_goal}

要求：不改变核心观点；不删除重要信息；让句子更自然；减少 AI 味；
保留个人表达感；不要过度煽情；不要改成营销文。

请只输出 JSON，不要输出 Markdown 代码块：

{
  "content": "润色后的完整正文 Markdown",
  "change_summary": "简要说明修改方向"
}
```

### 11.6 生成标题 Prompt

```
你是我的公众号标题策划助手。
文章正文：{content}

请生成 10 个公众号标题，分别覆盖不同风格。
要求：不标题党；不低俗猎奇；不夸大事实；能承接正文；有点击欲望。

请只输出 JSON，不要输出 Markdown 代码块：

{
  "titles": [
    {
      "text": "标题文本",
      "type": "思考型",
      "reason": "推荐理由",
      "risk": "风险提示"
    }
  ]
}
```

### 11.7 生成发布物料 Prompt

```
你是我的公众号发布助手。
文章标题：{title}
文章正文：{content}

请生成以下发布物料：
1. 公众号摘要（50～120 字，概括核心，略有吸引力）
2. 公众号封面文案（20 字以内）
3. 朋友圈转发文案（100 字以内，像作者自己写的推荐语，不要硬广）
4. 文章标签（5～8 个）

要求：文案自然；不要硬广；不要过度煽情；保持作者个人表达感。

请只输出 JSON，不要输出 Markdown 代码块：

{
  "summary": "公众号摘要",
  "cover_text": "公众号封面文案",
  "moments_text": "朋友圈转发文案",
  "tags": ["标签1", "标签2", "标签3"]
}
```

---

## 12. MVP 开发计划（修订版）

> **修订**：风格库基础配置提前至第 1 天，保证第 2 天开始的初稿生成都有风格基准。

| 阶段 | 主要任务 | 目标 |
|------|----------|------|
| 第 1 天 | 项目初始化 + DeepSeek 联通 + 设置页<br>· FastAPI + SQLite + Next.js<br>· `.env` 读取 API Key<br>· `/api/settings/test-api` 连通性测试 | 本地服务可运行，DeepSeek 调用成功 |
| 第 2 天 | 风格库 P0 + 一键直出接口<br>· 默认风格模板<br>· 粘贴 / 上传 1 篇样本文章<br>· `/api/write/quick-draft` 接入 Few-shot | 输入一个想法可生成带个人风格的初稿 |
| 第 3 天 | 首页 + 流式生成展示<br>· 首页输入框 + 方向选择<br>· 手动粘贴素材<br>· JSON 解析失败兜底 | 一键直出流程可稳定使用 |
| 第 4 天 | 写作编辑器<br>· 标题 / 正文 / 摘要 / 标签编辑<br>· 润色功能<br>· 自动保存草稿 | 生成后可继续修改，不丢内容 |
| 第 5 天 | 发布物料 + 标题生成<br>· 生成 10 个标题<br>· 生成摘要 / 封面文案 / 朋友圈文案 / 标签<br>· 一键复制 | 一篇文章可完成发布前准备 |
| 第 6 天 | 文章归档 + Markdown 导出<br>· 保存 / 查看列表 / 编辑 / 搜索<br>· 导出 Markdown<br>· 记录自评分 | 文章不再散落在临时文件里 |
| 第 7 天 | 整体打磨 + P1 取舍<br>· 错误提示 + 重试<br>· 空状态与加载态<br>· 若时间允许做素材箱最小版 | 形成可连续使用 7 天的 MVP |

---

## 13. 验收标准

**核心流程**：
- 输入一个想法，一键直出完整初稿
- 初稿包含标题、正文、摘要、标签，且能被成功解析为结构化数据
- 生成开始后 3 秒内出现流式输出或明确加载状态
- 生成失败时展示错误原因，并允许用户重试

**编辑流程**：生成结果可继续编辑；页面刷新后草稿不丢失；润色后可以保留原文或替换正文。

**发布流程**：一键生成 10 个标题和 4 类发布物料；每项物料可单独复制。

**风格流程**：上传或粘贴 1 篇历史文章作为 Few-shot 样本 → 设置默认风格 → 重新生成后，开头方式、句式长短或语气至少有一项接近样本。

**归档流程**：保存 / 查看历史文章 / 编辑 / 搜索 / 导出 Markdown。

**P1 验收（若实现）**：新增素材 → 搜索 → 从素材生成选题 → 将素材加入文章生成上下文。

---

## 14. 第一版成功指标

| 类型 | 标准 |
|------|------|
| 使用指标 | 连续使用 7 天；每天至少生成 1 篇草稿；归档 7 篇文章；至少导出 3 篇 Markdown |
| 效率指标 | 每篇文章从想法到初稿 ≤ 30 分钟（一键直出目标：≤ 10 分钟） |
| 质量指标 | 生成后正文改动比例 ≤ 30%；标题 10 个里至少 3 个可用；文章风格接近个人表达 |
| 黏性指标 | 用户愿意第二天继续打开工具 |

---

## 15. 后续迭代方向

**第二版**：
- 自动导出 Markdown
- 文章系列管理 + 历史文章相似选题推荐
- 手动录入公众号数据 + 数据复盘分析
- 文章开头库 + 结尾库
- 公众号排版模板
- 每周写作复盘报告

**第三版**：
- 多用户账号 + 云端同步
- 微信公众号草稿箱对接 + 热点选题抓取 + 文章数据自动同步
- 面向细分行业的写作模板
- 付费订阅功能

---

## 16. 产品原则

1. **不做万能 AI 工具**：只解决一个问题——帮助每日写公众号的人稳定完成一篇文章
2. **不追求一次生成完美文章**：AI 的任务是降低启动成本、整理思路、完成初稿
3. **个人风格优先**：核心价值是沉淀作者自己的风格、素材、选题和文章资产
4. **先自用，再产品化**：连续使用 30 天后，再考虑是否产品化

---

*创作工坊不是帮作者偷懒，而是帮作者把每天的表达变成一个稳定、可重复、可积累的系统。*

*整理：参宿ag*
